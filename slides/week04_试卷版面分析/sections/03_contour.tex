%===========================================================
% 03_contour.tex - 轮廓检测原理与API
%===========================================================

\section{轮廓检测}

\begin{frame}{轮廓 vs 边缘}
    \begin{columns}
        \column{0.5\textwidth}
        \begin{block}{边缘（Edges）}
            \begin{itemize}
                \item 不连续的像素集合
                \item 梯度变化的位置
                \item 原始数据
                \item 来自 Canny/Sobel
            \end{itemize}
        \end{block}

        \column{0.5\textwidth}
        \begin{block}{轮廓（Contours）}
            \begin{itemize}
                \item 连续的边界曲线
                \item 封闭的、有序的
                \item 可用于形状分析
                \item 来自 findContours
            \end{itemize}
        \end{block}
    \end{columns}

    \vspace{0.5cm}

    \textbf{关系：} 边缘 $\xrightarrow{\text{连接}}$ 轮廓

    \vspace{0.3cm}

    \begin{center}
        边缘图像 $\rightarrow$ 轮廓检测 $\rightarrow$ 轮廓列表
    \end{center}
\end{frame}

\begin{frame}{轮廓检测流程}
    \begin{enumerate}
        \item \textbf{输入}：二值图像（通常来自 Canny 或 threshold）
        \item \textbf{扫描}：遍历图像，寻找边界
        \item \textbf{连接}：将相邻边缘点连接成轮廓
        \item \textbf{层级}：建立轮廓之间的层级关系
        \item \textbf{输出}：轮廓点集 + 层级结构
    \end{enumerate}

    \vspace{0.3cm}

    \begin{alertblock}{注意}
        findContours 会修改输入图像！如果需要保留，请先复制。
    \end{alertblock}
\end{frame}

\begin{frame}[fragile]{课堂互动：错误找茬（第1步——找Bug）}
    \begin{block}{题目（集体讨论，3分钟）}
        以下代码运行后报错，请找出错误原因。
    \end{block}

    \pause

    \begin{columns}
        \column{0.55\textwidth}
        \begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
import cv2
img = cv2.imread('exam.jpg')
edges = cv2.Canny(img, 50, 150)
contours, _ = cv2.findContours(
    edges, cv2.RETR_EXTERNAL,
    cv2.CHAIN_APPROX_SIMPLE)
        \end{lstlisting}

        \column{0.45\textwidth}
        \begin{alertblock}{报错信息}
            \texttt{Canny(): argument 1}\\
            \texttt{must be image, not cv2.Mat}
        \end{alertblock}
    \end{columns}

    \pause

    \begin{uncoverenv}
        \begin{block}{提示}
            Canny 函数的输入图像，需要满足什么条件？
        \end{block}
    \end{uncoverenv}

\codesrc{code/contour\_detection.py :: detect\_contours()}
\end{frame}

\begin{frame}[fragile]{课堂互动：错误找茬（第2步——Bug定位）}
    \begin{columns}
        \column{0.5\textwidth}
        \begin{alertblock}{Bug 定位}
            \texttt{cv2.Canny(img, 50, 150)}\\
            $\uparrow$ \texttt{img} 是彩色图！\\
            $\Rightarrow$ \texttt{Canny} 需要灰度图。
        \end{alertblock}

        \column{0.5\textwidth}
        \begin{block}{正确代码}
            \begin{lstlisting}[basicstyle=\ttfamily\scriptsize, numbers=none]
gray = cv2.cvtColor(
    img,
    cv2.COLOR_BGR2GRAY)
edges = cv2.Canny(
    gray, 50, 150)
            \end{lstlisting}
        \end{block}
    \end{columns}

\codesrc{code/contour\_detection.py :: detect\_contours()}
\end{frame}

\begin{frame}[fragile]{课堂互动：错误找茬（第3步——经验总结）}
    \begin{block}{经验总结}
        \begin{enumerate}
            \item Canny / Sobel 等边缘检测 $\rightarrow$ 必须灰度图
            \item 读图后立即转灰度是好习惯
            \item 把灰度转换写在最前面，避免遗漏
        \end{enumerate}
    \end{block}

    \pause
    \vspace{0.3cm}

    \begin{uncoverenv}
        \begin{block}{类推练习}
            以下代码有没有问题？如果有，怎么改？
            \begin{verbatim}
img = cv2.imread('exam.jpg')
blur = cv2.GaussianBlur(img, (5,5), 0)
edges = cv2.Canny(blur, 50, 150)
            \end{verbatim}
        \end{block}
    \end{uncoverenv}
\end{frame}

\begin{frame}{三个理解层级：轮廓检测}
    \begin{columns}
        \column{0.33\textwidth}
        \textbf{基础概念：}
        \begin{itemize}
            \item 什么是轮廓？
            \item 轮廓与边缘的区别
            \item findContours的检索模式
        \end{itemize}

        \column{0.33\textwidth}
        \textbf{可视化演示：}
        \begin{itemize}
            \item 对比不同检索模式效果
            \item 绘制不同颜色的轮廓
            \item 调整逼近精度参数
        \end{itemize}

        \column{0.33\textwidth}
        \textbf{扩展应用：}
        \begin{itemize}
            \item 设计轮廓筛选算法
            \item 处理复杂嵌套轮廓
            \item 实现通用形状识别
        \end{itemize}
    \end{columns}
\end{frame}

\begin{frame}[fragile]{findContours API}
    \textbf{函数原型：}
    \begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
contours, hierarchy = cv2.findContours(
    image,              # input (binary image, white bg preferred)
    mode,               # contour retrieval mode
    method              # contour approximation method
)
    \end{lstlisting}

    \textbf{轮廓检索模式（mode）：}

\codesrc{code/contour\_detection.py :: detect\_contours()}
    \begin{table}
        \centering
        \small
        \begin{tabular}{ll}
            \toprule
            \textbf{模式} & \textbf{说明} \\
            \midrule
            cv2.RETR\_EXTERNAL & 只检测最外层轮廓 \\
            cv2.RETR\_LIST & 检测所有轮廓，不建立层级 \\
            cv2.RETR\_CCOMP & 检测所有轮廓，建立两级层级 \\
            cv2.RETR\_TREE & 检测所有轮廓，建立完整层级树 \\
            \bottomrule
        \end{tabular}
    \end{table}
\end{frame}

\begin{frame}[fragile]{轮廓逼近方法（method）}
    \textbf{存储方式：}
    \begin{table}
        \centering
        \begin{tabular}{ll}
            \toprule
            \textbf{方法} & \textbf{说明} \\
            \midrule
            cv2.CHAIN\_APPROX\_NONE & 存储所有边界点 \\
            cv2.CHAIN\_APPROX\_SIMPLE & 压缩水平、垂直、对角线段 \\
            cv2.CHAIN\_APPROX\_TC89\_L1 & Teh-Chin 链逼近算法 \\
            \bottomrule
        \end{tabular}
    \end{table}

    \begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
# common configuration
contours, hierarchy = cv2.findContours(
    binary,
    cv2.RETR_EXTERNAL,      # outer contours only
    cv2.CHAIN_APPROX_SIMPLE # compressed storage
)
    \end{lstlisting}

    \textbf{推荐：} RETR\_EXTERNAL + CHAIN\_APPROX\_SIMPLE

\codesrc{code/contour\_detection.py}
\end{frame}

\begin{frame}{层级结构（hierarchy）}
    \textbf{什么是层级？}轮廓之间的包含关系（外轮廓 vs 内轮廓）

    \textbf{hierarchy 数组格式：}
    $$
    [next, previous, first\_child, parent]
    $$

    \begin{itemize}
        \item \textbf{next}：同级下一个轮廓
        \item \textbf{previous}：同级前一个轮廓
        \item \textbf{first\_child}：第一个子轮廓
        \item \textbf{parent}：父轮廓
    \end{itemize}
\end{frame}

\begin{frame}{层级结构示例}
    \begin{columns}
        \column{0.4\textwidth}
        \centering
        \begin{tikzpicture}[scale=0.85]
            \draw[thick] (0,0) rectangle (3,3);
            \draw[thick] (0.5,0.5) rectangle (1.5,1.5);
            \draw[thick] (2,1) rectangle (2.8,2);
            \node at (1.5,3.3) {外轮廓 0};
            \node at (1,0.2) {内轮廓 1};
            \node at (2.4,0.7) {内轮廓 2};
        \end{tikzpicture}

        \column{0.6\textwidth}
        \textbf{hierarchy 示例：}
        \begin{table}
            \centering
            \tiny
            \begin{tabular}{cccc}
                \toprule
                next & prev & child & parent \\
                \midrule
                1 & -1 & 2 & -1 \\
                2 & 0 & -1 & 0 \\
                -1 & 1 & -1 & 0 \\
                \bottomrule
            \end{tabular}
        \end{table}

        \begin{itemize}\small\itemsep=0pt
            \item 轮廓0：外轮廓（无父级）
            \item 轮廓1、2：内轮廓（父级=0）
        \end{itemize}
    \end{columns}
\end{frame}

\begin{frame}[fragile]{绘制轮廓}
    \textbf{drawContours 函数：}
    \begin{lstlisting}[basicstyle=\ttfamily\small]
# 绘制所有轮廓
output = cv2.drawContours(image.copy(), contours, -1, (0,255,0), 2)
# 绘制第1个轮廓
output = cv2.drawContours(image.copy(), contours, 0, (0,0,255), 3)
    \end{lstlisting}

    \textbf{填充轮廓（thickness=-1）：}
    \begin{lstlisting}[basicstyle=\ttfamily\small]
output = cv2.drawContours(image.copy(), contours, -1, (0,255,0), -1)
    \end{lstlisting}

\codesrc{code/contour\_detection.py :: draw\_contours()}
\end{frame}

\begin{frame}[fragile]{轮廓检测完整示例}
    \begin{lstlisting}[basicstyle=\ttfamily\small]
import cv2
img = cv2.imread('exam.jpg')
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
blur = cv2.GaussianBlur(gray, (5, 5), 0)
edges = cv2.Canny(blur, 50, 150)
contours, _ = cv2.findContours(edges, cv2.RETR_EXTERNAL,
                                 cv2.CHAIN_APPROX_SIMPLE)
output = img.copy()
cv2.drawContours(output, contours, -1, (0, 255, 0), 2)
print(f"Found {len(contours)} contours")
cv2.imshow('Contours', output)
cv2.waitKey(0)
    \end{lstlisting}

\codesrc{code/contour\_detection.py :: contour\_detection\_pipeline()}
\end{frame}

\begin{frame}{AI辅助学习：向AI提问的技巧（一）}
    \textbf{场景1：理解Canny算法}
    \begin{exampleblock}{Prompt示例}
        请解释Canny边缘检测算法的4个步骤，并用简单的比喻帮助我理解。
    \end{exampleblock}

    \vspace{0.3cm}

    \textbf{场景2：调试边缘检测代码}
    \begin{exampleblock}{Prompt示例}
        我的Canny边缘检测代码报错了：[错误信息] \\
        运行环境：Python 3.9, OpenCV 4.8, Windows 11 \\
        相关代码：[粘贴代码] \\
        请帮我分析原因并提供解决方案。
    \end{exampleblock}
\end{frame}

\begin{frame}{AI辅助学习：向AI提问的技巧（二）}
    \textbf{场景3：优化轮廓检测}
    \begin{exampleblock}{Prompt示例}
        我用findContours检测到了很多轮廓，但想找到试卷轮廓。筛选条件：\\
        1. 面积是图像的50%-80\%  2. 是四边形
    \end{exampleblock}

    \vspace{0.4cm}

    \begin{block}{提问技巧总结}
        \begin{itemize}
            \item 说明环境和版本
            \item 粘贴完整代码
            \item 描述期望结果 vs 实际结果
            \item 明确说明编程语言和库版本
        \end{itemize}
    \end{block}
\end{frame}

\begin{frame}{轮廓筛选技巧（一）}
    \textbf{按面积筛选：}
    \begin{itemize}
        \item 排除过小的轮廓（噪声）
        \item 排除过大的轮廓（背景）
    \end{itemize}

    \textbf{按位置筛选：}
    \begin{itemize}
        \item 只保留图像中心区域
        \item 排除边缘区域的轮廓
    \end{itemize}
\end{frame}

\begin{frame}{轮廓筛选技巧（二）}
    \textbf{按形状筛选：}
    \begin{itemize}
        \item 按长宽比筛选
        \item 按凸包面积比筛选
        \item 按顶点数筛选（四边形=试卷）
    \end{itemize}

    \vspace{0.4cm}

    \begin{block}{组合策略}
        面积筛选 + 位置筛选 + 形状筛选 = 精准定位试卷
    \end{block}
\end{frame}
