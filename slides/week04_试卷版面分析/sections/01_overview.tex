%===========================================================
% 01_overview.tex - 版面分析概述与流程
%===========================================================

\section{版面分析概述}

\begin{frame}{什么是版面分析？}
    \begin{columns}
        \column{0.55\textwidth}
        \begin{block}{定义}
            从文档图像中识别和定位不同区域（标题、正文、表格、图片等）
        \end{block}

        \vspace{0.3cm}

        \textbf{在阅卷系统中的作用：}
        \begin{enumerate}
            \item 找到试卷边界（定位试卷）
            \item 定位选择题区域（OMR识别）
            \item 定位判断题区域（符号匹配）
            \item 定位简答题区域（手写识别）
            \item 定位填空题区域（内容提取）
        \end{enumerate}

        \column{0.4\textwidth}
        \begin{center}
            \includegraphics[width=\textwidth]{figures/答题卡.jpg}
        \end{center}
    \end{columns}
\end{frame}

\begin{frame}{为什么需要版面分析？}
    \begin{columns}
        \column{0.5\textwidth}
        \textbf{没有版面分析：}
        \begin{itemize}
            \item 不知道答题卡在哪
            \item 无法区分题型
            \item OCR识别范围过大
            \item 处理效率低下
        \end{itemize}

        \column{0.5\textwidth}
        \textbf{有了版面分析：}
        \begin{itemize}
            \item 精确定位各区域
            \item 分类处理不同题型
            \item 提高识别准确率
            \item 加快处理速度
        \end{itemize}
    \end{columns}

    \vspace{0.5cm}

    \begin{alertblock}{核心价值}
        版面分析是自动阅卷的"地图导航"！
    \end{alertblock}
\end{frame}

\begin{frame}{版面分析完整流程}
    \begin{center}
        \begin{tikzpicture}[
            pblock/.style={draw, rectangle, rounded corners=5pt,
                minimum height=1.1cm, minimum width=2.4cm,
                font=\small, align=center, line width=0.6pt},
            arr/.style={->, thick, >=stealth, gray!70},
            note/.style={font=\ttfamily\tiny, color=gray!55}
        ]
            % Top row (steps 1-4)
            \node[pblock, fill=blue!8] (input) at (0, 1.0)
                {\textbf{输入图像}\\[-1pt]{\itshape 试卷照片}};
            \node[pblock, fill=yellow!12] (pre) at (3.1, 1.0)
                {\textbf{预处理}\\[-1pt]{\itshape 灰度 + 降噪}};
            \node[pblock, fill=green!10] (edge) at (6.2, 1.0)
                {\textbf{边缘检测}\\[-1pt]{\itshape Canny}};
            \node[pblock, fill=red!8] (contour) at (9.3, 1.0)
                {\textbf{轮廓检测}\\[-1pt]{\itshape findContours}};

            % Bottom row (steps 5-7)
            \node[pblock, fill=orange!10] (filter) at (9.3, -0.9)
                {\textbf{轮廓筛选}\\[-1pt]{\itshape 面积 + 形状}};
            \node[pblock, fill=violet!10] (layout) at (6.2, -0.9)
                {\textbf{版面分析}\\[-1pt]{\itshape 投影法}};
            \node[pblock, fill=cyan!12] (output) at (3.1, -0.9)
                {\textbf{区域输出}\\[-1pt]{\itshape 题目定位}};

            % Step badges
            \node[circle, fill=blue!50, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]input.north west)   {1};
            \node[circle, fill=yellow!60, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]pre.north west)     {2};
            \node[circle, fill=green!50, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]edge.north west)    {3};
            \node[circle, fill=red!40, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]contour.north west) {4};
            \node[circle, fill=orange!50, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]filter.north west) {5};
            \node[circle, fill=violet!50, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]layout.north west) {6};
            \node[circle, fill=cyan!50, text=white, font=\tiny\bfseries,
                inner sep=1.5pt, minimum size=11pt]
                at ([shift={(-0.6, 0.4)}]output.north west) {7};

            % Function annotations
            \node[note] at ([yshift=-1mm]input.south)   {imread()};
            \node[note] at ([yshift=-1mm]pre.south)     {cvtColor + Blur};
            \node[note] at ([yshift=-1mm]edge.south)   {cv2.Canny()};
            \node[note] at ([yshift=-1mm]contour.south){findContours()};
            \node[note] at ([yshift=1mm]filter.north)  {approxPolyDP};
            \node[note] at ([yshift=1mm]layout.north)   {np.sum(axis=1)};
            \node[note] at ([yshift=1mm]output.north)   {connectedComponents};

            % Arrows
            \draw[arr] (input)    -- (pre);
            \draw[arr] (pre)     -- (edge);
            \draw[arr] (edge)    -- (contour);
            \draw[arr] (contour) -- (filter);
            \draw[arr] (filter)  -- (layout);
            \draw[arr] (layout)  -- (output);
        \end{tikzpicture}
    \end{center}
\end{frame}

\begin{frame}{版面分析方法分类}
    \textbf{传统方法：}
    \begin{itemize}
        \item \textbf{边缘检测}：Canny、Sobel
        \item \textbf{轮廓检测}：findContours
        \item \textbf{投影法}：水平/垂直投影
        \item \textbf{连通域分析}：blob检测
    \end{itemize}

    \vspace{0.3cm}

    \textbf{深度学习方法：}
    \begin{itemize}
        \item \textbf{目标检测}：YOLO、Faster R-CNN
        \item \textbf{语义分割}：U-Net、DeepLab
        \item \textbf{版面分析模型}：LayoutLM、DocFormer
    \end{itemize}

    \vspace{0.3cm}

    \begin{block}{本课程重点}
        传统方法（简单、高效、可控）
    \end{block}
\end{frame}

\begin{frame}{应用场景}
    \begin{columns}
        \column{0.33\textwidth}
        \begin{block}{教育阅卷}
            \begin{itemize}
                \item 答题卡识别
                \item 试卷自动批改
                \item 成绩统计
            \end{itemize}
        \end{block}

        \column{0.33\textwidth}
        \begin{block}{文档处理}
            \begin{itemize}
                \item 发票识别
                \item 表单提取
                \item 合同分析
            \end{itemize}
        \end{block}

        \column{0.33\textwidth}
        \begin{block}{档案数字化}
            \begin{itemize}
                \item 版面重建
                \item 区域提取
                \item 内容索引
            \end{itemize}
        \end{block}
    \end{columns}
\end{frame}
