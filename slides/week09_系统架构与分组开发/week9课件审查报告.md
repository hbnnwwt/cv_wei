# Week 9 课件审查报告

**审查日期：** 2026-02-06
**审查人：** 小柚子（AI助教）
**审查范围：** Week 9 - 系统架构与分组开发 tex课件

---

## 一、审查依据

根据《原则.md》文档，审查重点包括：

### 1. 核心设计原则
- ✅ **跨专业适应性**：分层任务设计（基础概念→可视化演示→扩展应用）
- ✅ **教室教学策略**：多屏协同、动静结合（45分钟调整一次教学形式）
- ✅ **AI编程辅助机制**：推荐Cursor、Claude Code等工具，提供70%完整度代码框架
- ✅ **Prompt工程规范**：RTF框架、思维链引导、多轮迭代

### 2. 第9周课程大纲要求
根据《原则.md》，第9周（3课时）应包含：
- **项目回顾（0.5课时）**：已学模块回顾
- **系统架构设计（0.5课时）**：模块化架构、接口设计、项目目录结构
- **分组与任务（0.5课时）**：分组原则、角色分工、开发任务清单
- **开发指导（0.5课时）**：环境搭建、调试工具函数
- **本周任务（1课时）**：确定分组、制定计划、搭建环境、开始模块开发

### 3. 时间分配要求
单次3课时（160分钟）应按以下模式：
- 理论讲解（20分钟）→ 实践环节（20分钟）→ 讨论（5分钟）
- Live Coding为核心，理论精简

---

## 二、课件内容审查

### ✅ 符合原则的内容

#### 1. **系统架构理论基础扎实**
- ✅ 软件架构概述、架构决策层次讲解清晰
- ✅ SOLID原则、KISS、DRY原则讲解到位
- ✅ 架构风格分类完整（单体、分层、微服务、事件驱动、管道-过滤器）
- ✅ 有对比分析和小型系统选型建议

#### 2. **架构设计模式应用**
- ✅ 分层架构详解（表现层、业务层、领域层、数据层）
- ✅ 微服务架构（包含API网关、服务拆分、独立部署）
- ✅ 管道-过滤器架构（适合图像处理流水线）
- ✅ 事件驱动架构（发布-订阅模式）

#### 3. **团队协作内容**
- ✅ 敏捷开发方法论（Scrum、Kanban）
- ✅ 团队协作工具（Git、GitHub、项目管理工具）
- ✅ 代码规范（PEP 8、Git提交规范）
- ✅ 冲突解决策略

#### 4. **智能阅卷系统应用**
- ✅ 业务场景分析（用户、痛点、流程）
- ✅ 功能需求分析（核心功能、辅助功能）
- ✅ 非功能性需求（性能、准确率、可扩展性、安全性）
- ✅ 典型架构案例展示

---

## ⚠️ 主要问题与改进建议

### 问题1：内容深度与课程定位不匹配 ⚠️

**问题描述：**
课件中包含了大量企业级架构设计内容（微服务、事件驱动、领域驱动设计、CQRS、事件溯源等），这些内容对于一门面向全校理工科学生的《计算机视觉》通识课程来说，过于深奥和超前。

**不符合原则之处：**
- 《原则.md》强调"跨专业适应性"、"门槛友好"、"背景知识补充"
- 第9周大纲要求的是"模块化架构、接口设计、项目目录结构"等基础内容
- 学生需要的是"能完成智能阅卷原型系统"，而不是成为架构师

**改进建议：**
1. **大幅简化架构理论**：保留分层架构和管道-过滤器（适合图像处理），删除微服务、事件驱动、DDD等高级内容
2. **聚焦实战需求**：将理论讲解缩减到20分钟内，重点放在如何设计模块接口、如何组织项目结构
3. **降低技术门槛**：用"乐高积木"类比架构设计，避免过多专业术语

### 问题2：缺少AI编程工具使用指导 ⚠️

**问题描述：**
课件中没有体现《原则.md》核心特色的"AI编程辅助机制"。

**不符合原则之处：**
- 《原则.md》要求"第2周系统学习AI编程工具"后，后续课程应该持续应用
- 要求提供"70%完整度的项目框架代码"
- 要求"建立课程专属AI助手，解答编程问题"
- 要求提供"Prompt工程规范（RTF框架、思维链、少样本提示）"

**改进建议：**
1. **增加Cursor/Claude Code实战演示**：
   - 演示如何用AI生成接口定义
   - 演示如何用AI生成单元测试
   - 演示AI辅助重构代码

2. **提供完整项目框架**：
   ```
   AutoGradingSystem/
   ├── core/
   │   ├── __init__.py
   │   ├── image_pipeline.py      # 流水线引擎（框架已实现70%）
   │   └── recognizer.py          # 识别器基类（框架已实现70%）
   ├── modules/
   │   ├── preprocess/
   │   │   ├── denoise.py         # TODO: 学生实现
   │   │   └── binarize.py        # TODO: 学生实现
   │   ├── analysis/
   │   │   └── layout.py          # TODO: 学生实现
   │   └── recognition/
   │       ├── choice.py          # TODO: 学生实现
   │       └── judge.py           # TODO: 学生实现
   ├── utils/
   │   ├── debug.py               # 调试工具（已提供）
   │   └── visualization.py       # 可视化工具（已提供）
   └── tests/
       └── test_pipeline.py       # 测试框架（已提供）
   ```

3. **提供AI Prompt模板**：
   ```markdown
   ### RTF框架示例
   **Role**: 你是一个Python架构师
   **Task**: 设计一个图像预处理流水线的接口
   **Format**: 包含类定义、方法签名、类型注解

   ### 思维链示例
   "请一步步思考：首先确定输入输出数据结构，然后定义过滤器接口，最后实现流水线引擎..."
   ```

### 问题3：缺少图像处理流水线实战代码 ⚠️

**问题描述：**
虽然有流水线架构的详细讲解，但缺少与智能阅卷系统紧密结合的实战代码。

**不符合原则之处：**
- 《原则.md》要求"Live Coding为核心"
- 要求"第9周提供70%完整度的项目框架代码"
- 要求"本周任务：开始模块开发（至少完成选择题框架）"

**改进建议：**
1. **增加完整的流水线实战代码**（应包含在08_pipeline_extended.tex中）：
   ```python
   # 已提供的框架代码示例
   class ImagePipeline:
       def __init__(self, name: str = "default"):
           self.name = name
           self.filters: List[FilterInterface] = []

       def add_filter(self, filter_obj: FilterInterface) -> 'ImagePipeline':
           self.filters.append(filter_obj)
           return self

       def execute(self, image: np.ndarray) -> ImageData:
           data = ImageData(image, {'pipeline': self.name})
           for filter_obj in self.filters:
               data = filter_obj.process(data)
           return data
   ```

2. **提供具体到阅卷场景的示例**：
   ```python
   # 构建阅卷预处理流水线
   pipeline = ImagePipeline("exam_preprocess")
   pipeline \
       .add_filter(DenoiseFilter(kernel_size=5)) \
       .add_filter(BinarizeFilter(method='adaptive')) \
       .add_filter(PerspectiveCorrectionFilter())

   # 执行处理
   result = pipeline.execute(exam_image)
   ```

### 问题4：时间分配与实际内容不匹配 ⚠️

**问题描述：**
课件内容过于庞大（理论约40页），但第9周只有3课时（160分钟），且要求"理论讲解精简，重点在动手实践"。

**不符合原则之处：**
- 《原则.md》要求"每45分钟调整一次教学形式"
- 要求"讲授20min→实践20min→讨论5min"
- 要求"Live Coding为核心"

**改进建议：**

**推荐时间分配（与《原则.md》第9周大纲对齐）：**

```
第1学时（45分钟）：
00:00-00:05  项目回顾（已学模块快速过一遍）
00:05-00:15  系统架构设计概述（精简讲解分层+管道）
00:15-00:35  Live Coding：搭建项目框架（用Cursor演示）
00:35-00:45  小组讨论：确定分工和开发计划

第2学时（50分钟）：
00:45-01:05  Live Coding：实现图像预处理流水线
01:05-01:25  实践环节：学生跟随编写代码
01:25-01:35  Git工作流演示（分支、提交、PR）
01:35-01:50  小组协作实践：推送到GitHub

第3学时（65分钟）：
01:50-02:10  Live Coding：实现选择题识别模块框架
02:10-02:25  分组任务分配（具体到每个人）
02:25-02:40  案例分析：优秀项目架构参考
02:40-02:45  总结与作业布置
```

### 问题5：缺少调试工具函数 ⚠️

**问题描述：**
《原则.md》明确要求提供"调试工具函数：save_debug_image、draw_debug_boxes"，但课件中没有体现。

**改进建议：**
增加专门的调试工具模块：
```python
# utils/debug.py
import cv2
import os
from datetime import datetime

def save_debug_image(image, name, output_dir="debug"):
    """保存调试图像"""
    os.makedirs(output_dir, exist_ok=True)
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    filename = f"{name}_{timestamp}.png"
    path = os.path.join(output_dir, filename)
    cv2.imwrite(path, image)
    print(f"[DEBUG] Saved: {path}")

def draw_debug_boxes(image, boxes, labels=None, color=(0, 255, 0)):
    """在图像上绘制调试框"""
    result = image.copy()
    for i, box in enumerate(boxes):
        x, y, w, h = box
        cv2.rectangle(result, (x, y), (x+w, y+h), color, 2)
        if labels and i < len(labels):
            cv2.putText(result, labels[i], (x, y-10),
                       cv2.FONT_HERSHEY_SIMPLEX, 0.5, color, 2)
    return result
```

---

## 三、内容完整性检查

### 缺少的关键内容

1. ❌ **项目目录结构示例**
   - 应展示完整的智能阅卷系统项目结构
   - 标注哪些是已提供的框架，哪些需要学生实现

2. ❌ **接口设计规范**
   - 模块间接口如何定义
   - 输入输出数据格式规范
   - 错误处理机制

3. ❌ **分组任务清单**
   - 高/中/低优先级任务分解
   - 每个角色的具体任务清单

4. ❌ **开发环境搭建指南**
   - 虚拟环境创建
   - 依赖安装（requirements.txt）
   - IDE配置（Cursor/Claude Code）

5. ❌ **AI辅助编程示例**
   - 如何用AI生成单元测试
   - 如何用AI调试错误
   - 如何用AI优化代码

---

## 四、与《原则.md》第9周大纲对比

| 要求项 | 课件内容 | 符合度 |
|--------|---------|--------|
| 项目回顾（0.5课时） | ❌ 未体现 | 0% |
| 系统架构设计（0.5课时） | ✅ 有但过深 | 60% |
| 分组与任务（0.5课时） | ⚠️ 有理论但缺少实践 | 50% |
| 开发指导（0.5课时） | ⚠️ 有工具介绍但缺少实战 | 40% |
| 本周任务（1课时） | ❌ 无具体任务清单 | 0% |
| Live Coding为核心 | ❌ 理论过多 | 20% |
| 动静结合（讲授20→实践20→讨论5） | ❌ 不符合 | 10% |
| 70%完整度代码框架 | ❌ 未提供 | 0% |
| 调试工具函数 | ❌ 未提供 | 0% |
| AI编程辅助 | ❌ 未体现 | 0% |

**总体符合度：约25%**

---

## 五、修改建议优先级

### P0（必须修改，影响课程定位）
1. **大幅简化架构理论**：删除微服务、事件驱动、DDD，聚焦分层+管道
2. **补充Live Coding内容**：至少占课程50%以上
3. **提供70%完整度项目框架代码**
4. **增加AI编程工具使用演示**
5. **调整时间分配**：符合20min讲授+20min实践+5min讨论模式

### P1（强烈建议，影响教学效果）
1. **补充项目回顾**：快速回顾第3-8周核心内容
2. **提供接口设计规范**：模块间如何协作
3. **补充分组任务清单**：具体到每个角色
4. **提供调试工具函数**：save_debug_image、draw_debug_boxes
5. **增加AI Prompt模板**：RTF框架、思维链示例

### P2（建议改进，提升体验）
1. **增加案例分析**：展示优秀的学生项目架构
2. **补充常见问题FAQ**：Git冲突、接口不匹配等
3. **增加互动环节**：架构设计小游戏、代码审查练习

---

## 六、推荐修改方案

### 方案A：快速调整（1-2天）
**适用场景：** 时间紧急，保留现有结构，精简内容

**主要修改：**
1. 删除微服务、事件驱动、DDD等高级内容（约15页）
2. 增加2-3页Live Coding实战代码
3. 增加1页项目目录结构示例
4. 增加1页AI Prompt模板

**页数：** 约45页

### 方案B：深度重构（3-5天）⭐ 推荐
**适用场景：** 有充足时间，完全符合《原则.md》要求

**主要修改：**
1. 重新设计课件结构，严格对齐第9周大纲
2. Live Coding占50%以上（约25页实战代码）
3. 提供完整项目框架代码（包含所有TODO标记）
4. 增加AI编程辅助演示（Cursor/Claude Code）
5. 补充调试工具函数和使用示例

**推荐结构（共约45页）：**

```
Part 1: 课程导入与项目回顾（3页）
- 已学模块快速回顾（1页）
- 本周目标与任务说明（1页）
- 分组原则与角色分工（1页）

Part 2: 系统架构设计精讲（8页）
- 什么是架构（1页）
- 分层架构（2页）
- 管道-过滤器架构（2页）
- 接口设计原则（1页）
- 项目目录结构（1页）
- 设计模式应用（1页）

Part 3: Live Coding实战（25页）⭐ 核心
- Live Coding 1：搭建项目框架（5页）
  - Cursor演示创建项目结构
  - 定义ImageData类
  - 定义FilterInterface接口
  - 实现ImagePipeline引擎

- Live Coding 2：实现预处理流水线（6页）
  - DenoiseFilter实现
  - BinarizeFilter实现
  - PerspectiveCorrectionFilter实现
  - 流水线组合与测试
  - AI辅助调试演示

- Live Coding 3：Git工作流实践（4页）
  - 创建GitHub仓库
  - 分支策略演示
  - 代码提交规范
  - PR流程演示

- Live Coding 4：分组协作实战（5页）
  - 任务分配工具演示
  - 小组分工实践
  - 调试工具函数使用
  - 集成测试演示

- Live Coding 5：选择题框架实现（5页）
  - ChoiceRecognizer基类
  - AI生成单元测试
  - 前后端接口对接
  - 完整流程演示

Part 4: 案例分析与互动（5页）
- 优秀项目架构参考（2页）
- 常见问题FAQ（2页）
- 互动Quiz（1页）

Part 5: 总结与作业（4页）
- 知识点总结（1页）
- 作业要求与评分标准（1页）
- 代码提交规范（1页）
- 延伸学习资源（1页）
```

---

## 七、总结

### 优点
1. ✅ 软件架构理论基础扎实，讲解清晰
2. ✅ 架构风格分类完整，有对比分析
3. ✅ 团队协作内容全面，包含工具和规范
4. ✅ 有详细的代码示例（流水线、过滤器）

### 核心问题
1. ❌ **课程定位偏差**：内容过于深奥，不符合通识课定位
2. ❌ **缺少AI编程辅助**：未体现课程核心特色
3. ❌ **Live Coding不足**：理论过多，实践过少
4. ❌ **代码框架缺失**：未提供70%完整度项目框架
5. ❌ **时间分配不当**：内容量与课时不匹配

### 最终评价
**整体符合度：25%**

课件质量较高，但与《原则.md》的课程定位存在较大偏差。建议采用**方案B（深度重构）**，大幅简化理论内容，大幅增加Live Coding实战，提供完整的项目框架代码，才能符合"AI辅助编程降低门槛、以实践为核心"的课程理念。

---

**审查人签名：** 小柚子🍊
**日期：** 2026-02-06
